# Faculty AI Copilot — Full End-to-End Walk

**Date:** 2026-08-24  
**Scope:** Investigation only (no code changes) — first live walk past Plan after the plan-fallback fix.  
**Actor:** `faculty@iscarb.edu`  
**Project:** `cmt6k6ikz002kitmg9n12wuf5`  
**Environment:** Next.js `:3000`, Docker Postgres `:5433`, Redis `:6380`  
**AI keys available to server:** `NVIDIA_API_KEY` (+ multiples), `OPENAI_API_KEY` present in `.env`  
**Evidence artifacts:** `scripts/_faculty_e2e_full_out.json`, `scripts/_faculty_e2e_full_log.txt`

---

## Verdict

**How far the workflow gets:** Through **Studio → Inbox → Quality Gates → Readiness → Alignment → Jaheziah → NCAAA**. All 20 slide artifacts persist. Coverage and alignment handoffs persist from real slides. Publish does **not** succeed.

**Next blocker:** Content-quality gates, not a broken publish pipeline.

1. **`density` + `student_experience` error gates** — 13 slides exceed the **40-word** max (content depth / generation verbosity vs. gate rule). After mass-approve, still `failedErrorGates: 2`.
2. **4 readiness items unapproved** — Inbox “Approve All” does **not** approve readiness checks; publish still requires them.

Publish returns **422 `PUBLISH_BLOCKED`** with those exact blockers. Gating is honest.

---

## Stage-by-stage table

| Stage | Completes? | Persists real data? | Lands in | Errors / notes | Verdict |
|-------|------------|---------------------|----------|----------------|---------|
| **Create project** | Yes | `LectureProject` + course profile | `draft` | — | **Works** |
| **CLO approve** | Yes | `cloApprovedAt` set | CLOs approved | — | **Works** |
| **Source upload + parse** | Yes | 1 HTML doc, **15** source blocks (6 critical) | parse `done` | — | **Works** |
| **Source Map** | Yes (API) | Blocks listed; summary `total: 15`, `mapped: 0`, `unresolved: 15` | `parseStatus: done` | Mapping disposition still unresolved until generate handoffs | **Partial** — parse works; faculty mapping UI not exercised; coverage later filled by generate |
| **Plan** | Yes | **20** `LectureSlidePlan` rows with real CLO ids (`c1`–`c3`) + real block cuids | `planning` → Redis job `done` | Fallback titles used (topic-grounded); ~45s AI wait then fallback | **Works** |
| **Plan approve** | Yes | All plans `approved`; project → `approved_plan` | `approved_plan` | `POST .../plan/approve` → 200 `readyToGenerate: true` | **Works** |
| **Studio (generate)** | Yes | **20/20** artifacts persisted | project `review`; job `done` | All land **`flagged`**; ~**1443s (~24.1 min)**; job error string: *"Needs faculty review due to QA failure"* (not a hard crash) | **Works (slow; all flagged)** |
| **Inbox** | Yes | Surfaces real artifact + alignment rows | `projectStatus: review` | `totalPending: 40` (20 artifact + 20 alignment); fake SKU label on alignment messages (see §Fabrication) | **Partial** — functions; dishonest alignment copy |
| **Quality Gates** | Yes | Gate results persisted; validate runs | 13 pass / **2 fail** | Failures: `density`, `student_experience` (word-count content, not broken runner) | **Works (content fails gates)** |
| **Readiness** | Yes | **4** real MCQ readiness items (slides 5,9,14,20) with stems/options/CLO ids | items `approved: false` | Rich, source-grounded content | **Works** |
| **Alignment matrix** | Yes | Mode-correct empty matrix | `COURSE_READINESS`, `rows: []` | No OFFICIAL fabrication on this path | **Honest** |
| **Jaheziah** | Yes | Eligibility row / response | `COURSE_READINESS` | Honest rationale: no approved Jaheziah snapshot for specialty | **Honest** |
| **NCAAA** | Yes | Empty requirements | `synced: false`, 0 met/gap | Honest empty catalog (no approved NCAAA snapshot) | **Honest** |
| **Publish** | No (blocked correctly) | No package version created | stays `review` | Before approve: 422 (gates + 20 unapproved slides + 4 readiness). After inbox approve-all: 422 (**2 gates** + **4 readiness**) | **Gating works; not publishable yet** |

---

## Studio generation (detail)

| Metric | Result |
|--------|--------|
| `POST /generate` | **202** (`slidesQueued: 0` = generate-all quirk; empty `slideNos` means all 20) |
| Duration | **1,443,141 ms ≈ 24.1 minutes** |
| Timed out? | **No** — Redis job reached `status: done`, `progress: 100` |
| Artifacts | **20/20** (slides 1–20) |
| Final statuses | **All `flagged`** (0 draft, 0 approved at generation end) |
| Job error field | `"Needs faculty review due to QA failure"` (completion with QA fail flag, not abort-before-persist) |
| Pipeline phases observed | `analysing_sources` (~3+ min) → `generating_blueprint` (~8+ min stuck at progress 8) → `blueprint_done` → `concept_cards_done` → `generating_slides` → `done` |

Pre-slide phases dominated wall time (~14 min before first artifacts). Slide writing produced mostly **flagged** rows as QA failed density/experience checks.

---

## Stage handoffs (coverage / alignment / NCAAA)

Queried from DB after generation completed:

| Handoff | Count | Real? | Sample |
|---------|-------|-------|--------|
| **Coverage links** | **20** | Yes — real block cuids, disposition `mapped` | `blockId: cmt6k6j3a002nitmgjptx5n0p`, slide 1 |
| **Alignment links** | **20** | Yes — real `cloId` + `artifactId`; `standardOutcomeId: null`; mode `COURSE_READINESS`; decision `pending` | Rationale: *"Course-readiness CLO-to-slide binding from the approved plan."* |
| **NCAAA evidence links** | **0** | Expected — no approved NCAAA requirements to bind | — |
| **Readiness items** | **4** | Yes — full MCQs with CLO + source block ids | Unapproved |

**Verdict:** Coverage and alignment handoffs **do** persist from real slides after generate. NCAAA rows correctly stay empty without official catalog.

---

## Publish path

### Before faculty approvals
```
GET publish-readiness → canPublish: false
blockers: ["2 error gate(s) not resolved", "20 slide(s) not yet approved", "4 readiness item(s) not approved"]
counts: failedErrorGates=2, unapprovedSlides=20, unapprovedReadinessItems=4, currentSlideCount=20

POST publish → 422 PUBLISH_BLOCKED (same blockers)
```

### After Inbox POST “Approve All”
```
approvedCount: 40 (artifactsApproved: 20, alignmentApproved: 20, coverageApproved: 0)
artifacts → all status "approved"
coverageApproved: 0 because coverage was already disposition "mapped" (not pending)

blockers remaining:
  "2 error gate(s) not resolved"
  "4 readiness item(s) not approved"

POST publish → 422 PUBLISH_BLOCKED
```

**Gate nature:** Content depth / verbosity vs. hard **40-word** rule — not a broken gate evaluator. Example findings: slide 6 = 115 words; slide 17 = 75 words. Same findings under both `density` and `student_experience`.

**Readiness gap:** No automatic approval of readiness items on inbox approve-all; faculty must approve readiness separately (or an API path we did not exercise) before publish.

---

## Fabrication issues (prior audit) — current live state

| Issue | Still present? | Evidence on this project |
|-------|----------------|--------------------------|
| **National-standards hardcoded SKUs** | **Yes** | `GET /api/iscarb/lecture/national-standards` → `synced: true` with `snapshot-bio-2026`, `snapshot-se-2026`, etc. (fabricated fallback; no real approved snapshots) |
| **Alignment-matrix OFFICIAL fabrication** | **Not triggered** | Project mode `COURSE_READINESS` → `rows: []` (honest). OFFICIAL fabrication path not exercised by live data flow |
| **Inbox fake SKU label** | **Yes (pre-approve)** | Pre-approve inbox had 20 `alignment` pending items; DB rows had `standardOutcomeId: null` + mode `COURSE_READINESS`. Inbox route substitutes `"SKU 8.2 Fundamentals of Software Security"` when id is null (`decisions/inbox/route.ts`). E2E summary `inboxFakeSkuLabel: true`. After Approve All, those items left the pending queue |
| **Jaheziah eligibility** | **Honest** | `COURSE_READINESS`, no invented specialty |
| **NCAAA evidence** | **Honest** | Empty, `synced: false` |

Real data flowing through Studio **does not** fix the national-standards or inbox label fabrication; those are independent dishonest API/UI strings.

---

## What works now (vs pre-Plan-fix audit)

- Fresh project can complete **Source → CLO → Plan (20) → Plan approve → Studio (20 artifacts)**.
- Handoffs write **real** coverage + alignment rows.
- Quality gates evaluate real content; publish API still blocks correctly.
- Readiness generation produces real assessment items.

## What still blocks production publish

1. Generation consistently produces **over-dense** slides → error gates never clear without edit/regen/waive policy change.
2. **Readiness approval** is a separate gate not covered by inbox approve-all.
3. Fabrication on **national-standards** and **inbox alignment labels** remains (honesty debt, not this run’s publish blocker).
4. Studio wall time (~24 min) is operationally heavy for faculty UX.

---

## Appendix — key timestamps

| Event | Time (UTC) |
|-------|------------|
| Start | 2026-08-24T01:29:49Z |
| CLO approved | 01:29:51Z |
| Plan done (20 slides) | ~01:30:47Z |
| Generate accepted | ~01:30:47Z |
| Studio done (20 flagged) | ~01:54:50Z (~24.1 min generate) |
| Publish still blocked after approve-all | 01:54:58Z+ |
| End | 2026-08-24T01:55:00Z |

*Report only — no application code was modified.*
