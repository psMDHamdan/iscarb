// Re-export from new location — update imports to @/validation
export {
  assessmentSubmissionSchema, assessmentCreateSchema, userCreateSchema,
  userUpdateSchema, loginSchema, signupSchema, portfolioUpdateSchema,
  reportRequestSchema, consentUpdateSchema, validateInput,
} from "@/validation";
